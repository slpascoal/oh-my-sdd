## Tasks

- [x] 1. Criar `lib/sensors/detect.js`: detecção de repo multi-stack (Node: scripts.test/tsconfig/eslint/biome; Python: pytest/mypy/pyright/ruff/flake8; Go: go.mod/golangci; Rust: Cargo.toml test/check/clippy; Java: pom/build.gradle) → proposta sensors.json com comandos REAIS resolvidos por stack; exportar `detectSensors(cwd)` e `defaultCommand(sensor)` (fallback).
- [x] 2. Crear `lib/sensors/evidence.js`: funciones `fingerprint(cwd)` (sha256 de git HEAD+porcelain, fallback mtimes), `readEvidence(slug, sensor)`, `writeEvidence(slug, evidence)`, `isValid(evidence, currentFp)`, `evidencePath(slug, sensor)`.
- [x] 3. Crear `lib/sensors/runner.js`: `runSensor(sensor, cmd, cwd)` con spawn no interactivo (child_process.spawn), timeout (default 300s, configurable), captura exitCode/durationMs/output truncado (4KB/50 líneas); pre-check de skip por artefacto ausente (tests-passing sin script test, typecheck-clean sin tsconfig, lint sin eslint); `runAll(slug, config, cwd)` que ejecuta cada sensor declarado, escribe evidencia y retorna resumen.
- [x] 4. Crear `lib/commands/sensor.js`: subcomandos `init` (detect + confirma + escribe config + garantiza .gitignore con `.oh-my-sdd/runtime/`), `run <slug>` (exit 0 si required pasaron, 1 si falló), `status <slug>` (tabla chalk: sensor/status/exit/duración/fingerprint válido). Registrar en `bin/oh-my-sdd.js`.
- [x] 5. Actualizar `skills/oh-my-sdd-implement/SKILL.md`: Fase 3 reescrita — antes del reporte final ejecutar `npx oh-my-sdd sensor run <slug>`; si required falla, bloquear reporte y mostrar salida; criterios sin evidencia ni nota manual → "pendente de verificación"; soportar nota manual en `.oh-my-sdd/runtime/sensors/<slug>/manual-checks.md`.
- [x] 6. Verificar CA-1 a CA-6: fixtures locales (repo temp con tests+tsconfig; comando fallando; comando inexistente; modificar fuente tras correr sensor) ejecutando `node bin/oh-my-sdd.js sensor ...` y `npx oh-my-sdd status` de instalación.
- [x] 7. Actualizar README.md (sección "Sensors e evidencia") y docs EN+PT (página/sección nueva + nav mkdocs.yml si aplica).
